var variabel1 = "hei"; // Old, dont use 99% of the time
let variabel2 = "hei"; // reasignable
const variabel3 = "hei"; // not reasignable

variabel2 = 10;
variabel3 = 10; // error

let count = 0; // Has to be let, since we are changing it

const num1 = prompt("input first number");
const num2 = prompt("input second number");

const result = num1 + num1;

const string = "text"; // strings are text
const number = 10; // numbers are numbers:)
const boolean = true; // booleans are true/false, on or off, 0 or 1
// objects grouping together information
// about one thing
const object = { name: "Ludvig", age: 30 };
const array = [1, 2, 3]; // list of "similar" things
